<?
class querydb{

    function querydb(){
      $this->app = "CHITS Query Browser";
      $this->version = "0.1";
      $this->author = "Alison Perez";    
    }
    
    function querycrit($sdate,$edate,$brgy,$frage,$toage,$gender,$id){           
      $sdate = trim($sdate);
      $edate = trim($edate);
      
      echo "<table>";
      if(empty($sdate) || empty($edate)):
        echo "<tr><td>Please supply a date.</td></tr>";
      else:
        
        
      
      endif;
      
      echo "</table>";
    }
}
?>